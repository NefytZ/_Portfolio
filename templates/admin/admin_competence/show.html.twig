{% extends 'base.html.twig' %}

{% block title %}Competence{% endblock %}

{% block body %}
    <h1>Competence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ competence.Nom }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ competence.Niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_competence_index') }}">back to list</a>

    <a href="{{ path('app_admin_competence_edit', {'id': competence.id}) }}">edit</a>

    {{ include('admin_competence/_delete_form.html.twig') }}
{% endblock %}
