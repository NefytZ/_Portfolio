<form method="post" action="{{ path('app_admin_hobbie_delete', {'id': hobbie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hobbie.id) }}">
    <button class="btn">Delete</button>
</form>
